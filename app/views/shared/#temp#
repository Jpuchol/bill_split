<%= form_for(current_user.members.build(user_id: usr.id)) do |f\
| %>
                <div><%= f.hidden_field :user_id %></div>
                <%= f.submit "Add", :type => :image, :src => "images/add-image.\
png", :size => "25x25" %>
                <% end %>
