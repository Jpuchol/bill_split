<table>
  <thead>
    <tr>
      <th>Email   </th>
      <th>    </th>
      <th>    </th>
    </tr>
  </thread>

  <tbody>
    <% @users.each do |usr| %>
      <tr>
        <td><%= usr.email %></td>
        <% unless current_user?(usr) %>
          <div id="member_form">
            <td><%=  %></td>
            <% if usr.member?(usr) %>
              <td>Member</td>
            <% else %>
              <td>Not member</td>
            <% end %>
          </div>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
