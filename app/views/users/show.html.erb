<p id="notice"><%= notice %></p>

<% provide(:title, @user.name) %>
<h1><%= @user.name %></h1>

<p>
  <strong>Name:    Email:</strong>
  <%= @user.name %>,  <%= @user.email %>,  <%= @user.imag %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
